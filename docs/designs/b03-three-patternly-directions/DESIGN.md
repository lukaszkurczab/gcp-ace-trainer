# B-03 — Three Patternly visual directions

## Status and authority

- Status: `X-09A RECORDED` — A and C are selected finalists; neither is
  approved visual authority.
- Figma source: TalkToFigma channel `wtk4hp8i`, board `10:92` —
  *B-03 • Patternly visual directions • DRAFT — awaiting X-09A*.
- Board annotation: node `10:92`, category `Interaction`; it records the
  Product Owner's X-09A selection and preserves the remaining approval
  boundary.
- Inputs: the B-02 collision audit and
  `docs/06-branding-and-style-direction.md`.

The frames are editable proposal material, not implementation authority. On
2026-08-10 the Product Owner selected A and C at X-09A. They may be deepened
only in B-04; X-09B must still select one complete finalist before any final
brand approval or implementation authority exists.

## Editable candidates

| Figma node | Direction | Distinct visual logic | Included proof surfaces |
| --- | --- | --- | --- |
| `10:93` | **A — Boundary Signal** | **X-09A selected finalist.** Dispersed modules become one bounded path; a `P` is a secondary negative-space reading. | App-icon study, wordmark treatment, Coding/GCP accents, first-value hero, available/unavailable practice state and store-tile rule. |
| `10:94` | **B — Resolved Group** | **X-09A rejected; retained only as decision provenance.** Separate units settle into one readable group; the mark is grouped structure before monogram. | Must not be used as an active reference, exported, or implemented. |
| `10:95` | **C — Focus Frame** | **X-09A selected finalist.** A frame makes one resolved group and next action legible; the mark is focus/boundary rather than a provider or terminal symbol. | App-icon study, wordmark treatment, Coding/GCP accents, first-value hero, available/unavailable practice state and store-tile rule. |

All three are deliberately different in composition and their interpretation
of the brand grammar. They share only the canonical constraints: one Patternly
brand; subordinate monochrome-capable track symbols; Dark/Light viability; no
provider mark, cloud/service glyph, node mesh, hexagon, terminal cliché,
mascot, certificate, seal, badge, or family sub-brand.

## Decision boundary

`PO-061` records the Product Owner's X-09A selection: **A — Boundary Signal**
and **C — Focus Frame** advance to B-04. B is no longer a current-looking
reference. Its editable Figma frame remains only to make the 3→2 decision
auditable.

B-04 must compare the two complete systems — mark, small icon, wordmark,
track relationship and product-state readability — rather than select from a
single colour or screen. Neither finalist may be translated into production
tokens, assets, components, app icon, Store asset, or target UI until the
Product Owner makes the one-of-two X-09B decision. The existing PKG-04A
interaction reference remains the only approved design reference for its
narrow interaction scope.
