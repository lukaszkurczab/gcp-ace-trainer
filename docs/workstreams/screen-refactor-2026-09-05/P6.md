# P6 — Progress recent activity

Status: complete in the shared checkout; no commit created. This slice keeps learning metrics and evidence calculations on `trainingAttempts`, while Progress recent activity is sourced from canonical terminal session records.

Implementation agent: `gpt-5.6-luna`, reasoning `max`.

## Decision and fit

The implementation scores 0.92 for objective and architecture fit, 0.90 for simplicity, 0.88 for risk, and 0.91 for maintainability (minimum 0.88). It reuses the existing Activity reader/model and result destinations, adds only the dependency seam needed for Home to pass its existing attempts promise, and keeps the change inside the approved Progress/Home/Activity scope.

## Changed contracts

- `loadActivitySessionRecords` accepts optional repository readers so Home can pass the existing `trainingAttemptsRead` promise; the default path remains unchanged. Session and attempt repository issues still throw `StorageReadError`.
- Progress consumes terminal records filtered to the active track, sorted newest first, and capped at 12 rows. Completed sessions with zero answers remain visible; active sessions and abandoned sessions without committed attempts remain excluded by the canonical reader.
- Every Progress row is a pressable canonical session row. The shared typed route helper preserves the existing three destinations: coding simulation summary, coding practice summary, and generic result.
- Home wires the canonical records and row callback into Progress. ActivityScreen uses the same route helper. Home’s shell error path remains explicit and retryable; Activity’s owner continues to guard blur and retry generations.

## Files

- `src/application/activityReadModels.ts`
- `src/application/activityReadModels.test.ts`
- `src/features/home/activityNavigation.ts`
- `src/features/home/activityNavigation.test.ts`
- `src/features/home/ActivityScreen.tsx`
- `src/features/home/HomeScreen.tsx`
- `src/features/home/tabs/activityModel.ts`
- `src/features/home/tabs/homeProgressProjections.test.ts`
- `src/features/home/tabs/progressPresentation.test.ts`
- `src/features/home/tabs/progressTabModel.ts`
- `src/features/home/tabs/ProgressTab.tsx`

The obsolete attempts-only Progress activity item shape and its nested/date/outcome assertions were removed from `progressTabModel` and `homeProgressProjections.test.ts`. No storage, domain, locale, Settings, or full-QA files were changed by this slice.

## Verification

- `npm run typecheck` — PASS.
- `node --import tsx --test src/application/activityReadModels.test.ts src/features/home/activityNavigation.test.ts src/features/home/tabs/activityModel.test.ts src/features/home/tabs/homeProgressProjections.test.ts src/features/home/tabs/progressPresentation.test.ts` — 31/31 PASS.
- `node --import tsx --test src/application/runtimeAuditabilitySurfaces.test.ts src/features/home/homeOnboardingTransition.test.ts src/features/home/selectTrackFailureHandling.test.ts src/features/home/goalCadencePresentation.test.ts src/features/practice/practiceNavigation.test.ts` — 17/17 PASS.
- `git diff --check` — PASS.

Native, `npm run qa:static`, and full repository tests remain parent-owned gates.
