# P3 / P4 — dodatkowe dowody do wykonania

## P3 notifications
Inputs hook useNotificationSettings, notificationPreferences application, expoNotificationPlatform, NotificationSettingsScreen, EN/PL notifications, tests. Native confirmed: permission undetermined still titled Checking notification permission while Enable notifications button visible. Current copy maps null and undetermined together. Failures in refresh/request/save/disable/openSettings must be caught and visible; load/busy disables unsafe actions; single-flight synchronous guard, foreground refresh after OS settings and prevent stale reads overwriting completed mutation. Do not rewrite application scheduler/rollback or persistence. UI should retain entered invalid time, show local validation and save failure, disable controls while saving, clear draft errors on next open, display denied appropriately. Input height and keyboard must not hide CTA at large text. Use existing components and narrow domain state helper only if needed for behavior tests; no global async framework.

## P4 Settings/info
Inputs SettingsTab, AppearanceSettingsScreen/PreferenceSelectionScreen, YourData, Legal, SettingsInformationScreen, InfoBlock, SettingsGroup, SettingsBottomSheet, ChoiceRow, all consumers and related tests.
- Settings practiceSettingsDetail literal '20 items · After each answer' is misleading: HomeScreen opens PRACTICE_SETUP with only trackId, no persisted global practice preference. Use honest generic session setup description EN/PL; do not invent preference persistence.
- Appearance duplicate locale map should use appearance namespace; actual save needs error catch, busy/disabled feedback and retry/select; ChoiceRow current API inspect before editing.
- Version/build literal should be replaced using already available real runtime manifest/build config if reliable, else omit unknown field (no package addition merely for label).
- SettingsGroup with dividers currently nests individually rounded grouped rows + separators; improve coherent grouped surface within opt-in existing semantics, verify other users. Preserve tokens.
- SettingsInformationScreen holds translated topic object; use stable identity/position and derive current locale content. Show trailing chevron and inside-modal close. Do not make unproven legal promises. YourData/Legal unused locale remove.
- Native confirmed: SettingsBottomSheet has only backdrop 'Close' outside accessibilityViewIsModal; Maestro cannot locate Close, although coordinate tap on backdrop works. Add visible accessible close inside modal, wrapping title at 200%. Review keyboard-aware modal for notifications. This is real shared reuse (filter, reminder, info), not screen framework.
- YourData copy is overly implementation-specific and contains potentially conflicting 'no backup or restore path' near account restore description. If simplifying wording, verify account contracts and retain precise existing semantics; avoid broad legal rewrite. Add clear existing Account navigation only if current data view otherwise omits promised management and repo confirms destination.

Before/after snapshots and failures listed in visual-evidence.md. Test regex geometry alone is not native QA. Worker reports model gpt-5.6-luna/max, scale min>=.8, file/ref hygiene and exact tests.
