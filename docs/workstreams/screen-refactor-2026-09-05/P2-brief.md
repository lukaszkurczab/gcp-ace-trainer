# P2 — Activity execution packet

Inputs: plan.md, AGENTS.md, src/application/activityReadModels.ts, src/features/home/ActivityScreen.tsx, tabs/activityModel.ts + activityPresentation.ts + tests, trainingSessionResultRepository.ts/canonicalRecordCodec.ts.

Observed facts: ActivityScreen load lacks cleanup; retry missing; UTC date groups vs local clock. relativeDay is also used by HomeTab and progressTabModel, so common date correction must preserve their contract and run their tests. DateLabel assembled in model and parsed by translateDateLabel in screen — replace fragile English splitting with explicit localized formatter. Filter has two separate controls opening same sheet; keep one clear accessible trigger and independent clear button. Group 'This week' currently includes 7 days ago, which is not necessarily this calendar week; choose explicit honest period label or calendar-week semantics, with tests and localization. Results are independently keyed canonical records; no batch result API observed. Current getTrainingSessions/getTrainingAttempts throw canonical corruption rather than populate issues in ordinary path; respect returned issues contract without introducing broad storage rewrite.

Acceptance: canonical terminal filtering/completed zero answers/abandoned-with-attempt preserved; failures not fake empty; old focus loads cannot publish after blur/retry; newest retry wins; local-day grouping tested across midnight/DST; selected app locale controls month names and relative labels; results route unchanged; filter/empty/list readable at 200% and both palettes. Preserve family/model facts and no generic screen framework.

Tests: add behavioral read-model tests using existing memory storage test support; test date logic with local Date constructors and explicit TZ runs. Keep existing tests of completed/abandoned/time-expired/filter. Add lifecycle test only if testable contract mirrors real screen use, not regex-only fake coverage. typecheck and focused related Home/Progress consumers.

Output: P2.md with gpt-5.6-luna/max, files, removed paths/references, exact results and risks. Do not mark native visuals complete until controller captures final build.

Additional confirmed native/code gap: both HomeTab and ProgressTab hide Activity entry when active track has no progress. ProgressTab lines 176+ renders no onOpenActivity in !model.hasData branch; native capture cannot reach empty Activity via either tab. Global Activity may contain another track's history. Extend P2 file scope narrowly to ProgressTab empty branch: expose existing onOpenActivity via secondary action/runtimeSelectors.progress.activity(), preserve Open Practice primary. No dashboard model rewrite. Acceptance: all-track history reachable even when selected track has no evidence.
