# CNT-05 AZ-104 immutable release — 2026-08-20

Status: `PARTIAL`

## Scope

Rebuild the existing AZ-104 published bank from the current clean content branch and publish a new immutable release without changing semantic question content, mutating the historical release, or granting application admission.

## Evidence

- Content branch: `master`, pushed commit `fc48cb0` (the release publication is `d8d3f83`; the follow-up contract test is included in the current branch).
- Source identity used by the readiness report: `e73c7314eee7b2cd3f53b04c952b6af6526d3685` (the latest semantic source commit; later commits only carry technical evidence and immutable release records).
- Publication commit: `ad6cbe1b6948ddb16b7c9f7f3a26ddb49c12e0de`.
- Rebuilt artifact checksum: `968386e75c9abd4b54401e9876dadba6c0dbd01003aea8cfcad3a8d7027569ec`.
- New immutable release: `patternly-az104-0002`.
- Historical release `patternly-az104-0001` remains untouched.
- Existing bundled Free node remains `microsoft-azure-administrator-associate-az-104-free-node-0001`, 132 items, with provenance `patternly-az104-0001`.
- The deterministic readiness generator completed with eight tracks and now reports the AZ-104 immutable artifact as `verified`.

## Verification

- `npm run generate:eight-track-launch-readiness` passed and produced the deterministic report.
- `verifyArtifactRecord` passed for the new AZ-104 artifact.
- `npm run release:gate` remains `not_ready` by design; AZ-104 now has no `immutable_full_package_missing` blocker, but still has `publishing_admission_missing` and `runtime_admission_missing`.
- The current gate blocker counts are: application lock scope mismatch `1`, external evidence missing `6`, Free-node package missing `4`, immutable full package missing `6`, publishing admission missing `8`, runtime admission missing `8`.

## Non-goals and remaining blockers

This slice does not change the application lock, publish/runtime admission, entitlement, provider storage, or signed release state. GCP still requires a current-source replacement release; AWS and the three Design Interview tracks still lack Free-node/package contracts or artifacts. Human approval remains bound to the approved semantic source and is not regenerated by this evidence-only publication.
