# Decision register — implementation evidence

2026-09-04. MOB-DEC-001–009 are implemented and removed from the active register. The expanded loading scope, including tab/session transitions and the additional requested screens, is verified in the final section below. See the [completed loading plan and ownership record](loading-state-qualification.md). Earlier evidence and its limits are retained; the Answer review spacing change remains implemented.

## Result and canonical ownership

| Area | Final behavior and primary changed files |
| --- | --- |
| Account synchronization — 001 | `src/application/account/accountDataService.ts`, `AccountSessionProvider.tsx`, `src/features/home/HomeScreen.tsx`: actual Home focus requests the central pending-data retry. Durable binding, `offlinePending`, active-session, journal, materialization and adoption checks protect the existing `synchronizeBoundAccount` path. Concurrent requests share one attempt. UID checks protect provider updates. Upload/download interruptions caused by a new local session preserve durable pending state and remain retryable after completion. |
| Home — 003, 004, 006, 007 | `src/features/home/tabs/HomeTab.tsx`, `activityPresentation.ts`: removed Overview rails and Recent activity secondary time copy; retained metrics, activity title/empty state and actions. Course uses existing heading typography (20/28/700). Measured action columns share a center axis, including large-text stacking. Large-text Overview rows stack labels above values. |
| Practice — 008, 009 | `src/features/practice/PracticeHubScreen.tsx`, `PracticeSetupScreen.tsx`, `practiceFlowModel.ts`, `src/components/ListRow.tsx`, EN/PL common locales: stronger course title, explicit Active topic relationship, removed secondary mode helper descriptions, Knowledge Check display name. Operational unavailability reasons remain explicit. Mode titles can wrap completely. |
| iOS automation — 002 | `.maestro/completed-practice-result-review.yaml` performs all 10 answers and calls `coding-practice-result-review.yaml` to enter the read-only review. Existing M2 reuses it and then calls `coding-practice-answer-review-details.yaml`, retaining the extended review and relaunch assertions. `rc-algorithms-bootstrap.yaml` handles current guest/onboarding and explicit track confirmation. Navigation waits/retry and review scrolling use actual UI boundaries. `.gitignore` includes the new reusable flows. |
| Owner spacing correction | `src/features/practice/AlgorithmsPracticeSummaryScreen.tsx`: only `feedbackItems.gap` changes from 16 to 4. This removes extra spacing between rows while retaining original row padding, question/status spacing, typography, minimum height, complete text and navigation. Standard and maximum-text screenshots show the fourth question fully. |
| Loading — 005 | Screen-owned skeletons cover the expanded Home/tab, Practice, bootstrap, roadmap/setup, Goal/exam/review and simulation loading inventory. `SkeletonShape.tsx` supplies shared native glass motion with accessibility/lifecycle gating. Identity restoration and durable commands retain truthful, improved status presentations. Final native navigation, visual matrix, motion and regression evidence is recorded below. |

## Verification

All paths below are relative to the repository. Execution artifacts are in [the evidence directory](../artifacts/maestro-screen-capture/decision-register/2026-09-03/manifest.json).

- **iOS required path: PASS, exit 0.** `completed-review-run.yaml` runs the reset completion check, Algorithms bootstrap and the complete practice/result/read-only-review flow. The test checks the result, selected reviewed question, review root and absence of session submit/leave controls. [Execution log](../artifacts/maestro-screen-capture/decision-register/2026-09-03/logs/completed-review-core.log), [review screenshot](../artifacts/maestro-screen-capture/decision-register/2026-09-03/completed-review-core/read-only-review.png).
- **Account/Home: 30/30 PASS.** `node --import tsx --test src/application/account/accountLifecycle.test.ts src/features/home/homeOnboardingTransition.test.ts`. Includes real durable terminal commits, exactly one upload, failed-attempt coalescing, initial guards, and paused upload/download with concurrent local session start, completion and successful retry. Home wiring is a source contract. [Log](../artifacts/maestro-screen-capture/decision-register/2026-09-03/logs/sync-final-tests.log).
- **UI/model/contracts: 57/57 PASS.** Activity model, practice model/copy, Home large text/onboarding, runtime surfaces, visual shell, Algorithms accessibility and M2/core-journey contracts. [Log with individual test names](../artifacts/maestro-screen-capture/decision-register/2026-09-03/logs/ui-final-tests.log).
- **Final presentation/navigation/flow contracts: 20/20 PASS.** `node --import tsx --test src/components/visualShell.test.ts src/tracks/coding-interview/algorithmsPracticeSummaryNavigation.test.ts src/tracks/coding-interview/maestroM2Custom10AtSessionEnd.test.ts src/testing/userTestingCoreJourneyMaestro.test.ts`. [Log](../artifacts/maestro-screen-capture/decision-register/2026-09-03/logs/final-presentation-tests.log).
- **Final typecheck and whitespace: PASS.** `npx tsc --noEmit`; `git diff --check`.
- **Visual checks: PASS.** iPhone 17, iOS 26.4, English, dark saved app appearance, standard text and `accessibility-extra-extra-extra-large`. Home/Practice capture is `.maestro/home-practice-presentation.yaml`. [Home large](../artifacts/maestro-screen-capture/decision-register/2026-09-03/screenshots/home-details-dark-large.png), [Practice large](../artifacts/maestro-screen-capture/decision-register/2026-09-03/screenshots/practice-header-dark-large.png), [review spacing standard](../artifacts/maestro-screen-capture/decision-register/2026-09-03/screenshots/review-spacing-dark-standard.png), [review spacing maximum](../artifacts/maestro-screen-capture/decision-register/2026-09-03/screenshots/review-spacing-dark-large.png). Simulator text size restored to `large` (standard).

Counts overlap and must not be added together.

## Deletions and remaining paths

Removed unused rail values/styles, activity-time helper/styles and its obsolete test, dead helper-copy translation keys, and stale source-style assertions. Quiz/review commands were moved into shared executable flows, not duplicated. Updated tests include `activityModel.test.ts`, `practiceFlowModel.test.ts`, `practiceCopy.test.ts`, `homeTabLargeText.test.ts`, `runtimeAuditabilitySurfaces.test.ts`, `visualShell.test.ts`, `algorithmsSessionAccessibility.test.ts` and the two Maestro contract tests.

Manual account recovery remains necessary for explicit account error/adoption/recovery states; it is not a second upload implementation. Persisted mode identifiers and currently referenced translation keys remain because the runtime/result paths still use them; their display values are updated. The primary Practice explanation and actual unavailable-state reasons remain useful product content.

Spacing changes that clipped maximum-size text were rejected after comparison with the original source. Ineffective remount and flex changes were removed. The final correction is one list-gap value; no extra layout path or abstraction remains.

## Limits and independent review

The whole M2 suite is **not reported as passing**. An earlier run completed the result/review path and later failed at app relaunch with “App crashed or stopped”. Later extended Details interaction assertions also failed during device runs. Those assertions remain executable in M2; the required entry-flow PASS does not certify those extra interactions. [M2 execution](../artifacts/maestro-screen-capture/decision-register/2026-09-03/logs/m2-final.log), [extended interaction execution](../artifacts/maestro-screen-capture/decision-register/2026-09-03/logs/review-spacing.log).

The sync tests use the real durable repositories and a controlled API. They do not mount React Navigation or synchronize a real bound account on the simulator. Those earlier account/Home/Practice checks did not include Android or light-appearance device verification; newer loading-specific light/dark evidence is recorded below. The directory named `ui-light-standard` records a requested system setting, but the app retained its dark preference; exported screenshots are correctly labeled dark.

Independent implementation validation and QA used **gpt-5.6-luna, reasoning max**. Accepted minimum scores: initial UI 0.85; final sync repair 0.84; core/extended flow separation 0.89; final spacing 0.95; conditional register cleanup 0.92. Final account QA and bounded spacing QA: PASS. The minimum score is the lowest dimension, not an average. The later owner approval resolved the qualification dependency. The subsequent glass-motion implementation gate scored 0.82; final independent code QA passed.

## Screen-specific glass skeletons — 005

Implemented after owner approval, including the later explicit request for moving glass. Each of `ActivityScreen.tsx`, `AlgorithmsScopeSelectionScreen.tsx`, `ExamReviewScreen.tsx`, `ResultScreen.tsx`, `AlgorithmsPracticeSummaryScreen.tsx` and `MistakesReviewScreen.tsx` owns its existing pending geometry. Activity and Scope use the same outer gaps as their ready layouts. Results stack outcomes at large text; practice results do not invent optional review content. Error, empty, request-key and navigation branches are unchanged.

`SkeletonShape.tsx` and its barrel export share decoration only: one native 1800 ms loop per region, clipped SVG gradient bands, theme-aware intensity, noninteraction animation and no per-frame JavaScript layout work. Reduce Motion, unresolved/failed preference reads and inactive AppState disable motion. Cleanup stops/resets it, and newer preference events cannot be overwritten by stale queries. Shapes stay hidden from accessibility under each screen's single busy progressbar.

- **23/23 tests and typecheck: PASS.** `node --import tsx --test src/navigation/loadingStateOwnership.test.ts src/i18n/practiceCopy.test.ts`; `npm run typecheck`; `git diff --check`. [Tests](../artifacts/maestro-screen-capture/skeleton-glass/2026-09-03/logs/tests.log), [typecheck](../artifacts/maestro-screen-capture/skeleton-glass/2026-09-03/logs/typecheck.log).
- **iOS component matrix: PASS, both Maestro runs exit 0.** Six screens × light/dark × standard/maximum text; 36 full screenshots including scrolled large layouts. [Manifest](../artifacts/maestro-screen-capture/skeleton-glass/2026-09-03/manifest.json), [standard run](../artifacts/maestro-screen-capture/skeleton-glass/2026-09-03/logs/standard.log), [large run](../artifacts/maestro-screen-capture/skeleton-glass/2026-09-03/logs/large.log).
- **Motion: PASS.** [Video](../artifacts/maestro-screen-capture/skeleton-glass/2026-09-03/glass-motion.mp4). Two frames differ in 2,936 of 6,750 pixels inside the filter skeleton bar, excluding ambient decoration and system chrome. With native Reduce Motion enabled, comparison of 2,436,120 content pixels shows zero changes. [Moving comparison](../artifacts/maestro-screen-capture/skeleton-glass/2026-09-03/motion-diff.json), [reduced-motion comparison](../artifacts/maestro-screen-capture/skeleton-glass/2026-09-03/reduced-motion-diff.json).
- **Independent code QA: PASS**, `gpt-5.6-luna`, reasoning `max`. Final motion gate dimensions: consistency 0.94, simplicity 0.87, risk 0.82, maintainability 0.90; minimum 0.82.

Removed the six obsolete `LoadingState` imports/usages, replaced decorative leaf Views with the shared glass primitive, and deleted the two now-unreferenced loading-description keys from EN/PL locales. No new dependency, universal skeleton layout, production preview route, loading delay or compatibility path was added. The earlier Answer review spacing fix remains intact.

Visual evidence mounts the actual exported skeleton components with real preferences, Screen and retained headers in an ignored test entry; it does not exercise every production navigation transition. Developer LogBox chrome is omitted only in that entry. The local Expo dependency bootstrap was restored byte-for-byte ([checksum](../artifacts/maestro-screen-capture/skeleton-glass/2026-09-03/bootstrap-restoration.json)); standard text, dark appearance and the original disabled Reduce Motion setting were restored. Android, spoken VoiceOver output and instrumented background performance were not tested; the earlier full-M2 limitations above remain unchanged.

## Expanded loading scope — 005 completed, 2026-09-04

The earlier six-screen implementation did not cover all pending route branches. The expanded change adds screen-owned skeletons for Home/Progress/Settings, Practice Hub, all practice preparation entry paths, content bootstrap, roadmap, setup, Goal, exam, practice answer review, ordinary/simulation answer review, simulation preparation and verified simulation results. Setup geometry follows the explicitly known mode; unresolved data never invents topics, counts, correctness or completion.

The repeated-tab defect was caused by route parameters retaining an older tab while local tab state changed. Home now synchronizes those parameters. Exam reads additionally reject stale route generations and older completions, using a published sequence watermark that permits slow overlapping reads to progress. Simulation summary/review share one keyed focus/read hook, invalidate retry tokens on blur/unmount, and expose a resolved scoreless result as verification unavailable instead of endless loading. The existing navigating lifecycle state has an explicit locked operation presentation.

Primary changed owners are `HomeScreen.tsx` and its three tab files; `PracticeHubScreen.tsx`, `PracticeSessionSurface.tsx`, certification/design practice entry screens, `PracticeSetupScreen.tsx`, `TopicRoadmapScreen.tsx`, `AlgorithmsPracticeReviewScreen.tsx`; `ContentPreparationGate.tsx`; `GoalCadenceScreen.tsx`, `ExamScreen.tsx` and `examReadOwner.ts`; `ReviewLoadingSkeleton.tsx`, `AnswerReviewScreen.tsx`; simulation session/result/operation files and `simulationResultRead.ts` / `simulationResultReadOwner.ts`; `RootNavigator.tsx`, shared loading/Button components and EN/PL copy. Focused tests cover their ownership and meaningful asynchronous boundaries. The previous Answer review list gap remains 4 with original row padding and inner spacing.

Removed the startup LoadingState variant and duplicate animation/preference handling, old generic pending branches, practice completion icon/spacer layout, duplicate result-read logic and review geometry, duplicate native review header, unused loading styles/copy and superseded guards. The only two remaining generic status owners are authentication restoration and practice completion; they retain true operation copy with the shared glass band. Account recovery/sync/sign-out/delete cards remain because they describe real commands and unavailable/retry states. Reference checks find the removed startup strings only in a negative regression assertion. All new runtime helpers have live consumers; no production capture route or delay was added. Unrelated concurrent content/release work was preserved.

### Final evidence

Evidence root: [loading-state manifest](../artifacts/maestro-screen-capture/loading-states/2026-09-04/manifest.json).

- **Focused tests: 115/115 PASS, zero failures.** [Exact 20-file list](../artifacts/maestro-screen-capture/loading-states/2026-09-04/logs/test-files.txt), [execution log](../artifacts/maestro-screen-capture/loading-states/2026-09-04/logs/tests.log). These include controlled deferred reads for route changes, unmount, retry/blur ownership, reverse completion ordering and nonstarving slow overlaps, plus presentation/navigation/operation contracts. Counts overlap earlier runs and must not be added together.
- **Typecheck and whitespace: PASS.** `npm run typecheck`, `git diff --check`. [Typecheck log](../artifacts/maestro-screen-capture/loading-states/2026-09-04/logs/typecheck.log), [reference/dead-path checks](../artifacts/maestro-screen-capture/loading-states/2026-09-04/logs/reference-check.json).
- **Actual iOS navigation: PASS, Maestro exit 0.** Cold launch, repeated Home–Practice–Progress switching including the stale-parameter sequence, roadmap/setup entry, Goal entry, and existing-session resume. The active Claude Focus Practice session was preserved without submitting answers, ending, abandoning or replacing it. [Flow](../artifacts/maestro-screen-capture/loading-states/2026-09-04/navigation/navigation.yaml), [log](../artifacts/maestro-screen-capture/loading-states/2026-09-04/navigation/run.log), [video](../artifacts/maestro-screen-capture/loading-states/2026-09-04/navigation/navigation.mp4). Extracted frames show real [bootstrap](../artifacts/maestro-screen-capture/loading-states/2026-09-04/navigation/bootstrap-opening-storage.png) and [Preparing session](../artifacts/maestro-screen-capture/loading-states/2026-09-04/navigation/preparing-session.png). The recording does not establish that every brief pending branch was visually sampled.
- **Native component matrix: PASS.** 22 presentations × light/dark × standard/maximum text, with top and scrolled-bottom screenshots at maximum size: 132 screenshots. iPhone 17, iOS 26.4, English, fontScale 1.00/3.57; roadmap also tested in a 320 pt content viewport. [Standard log](../artifacts/maestro-screen-capture/loading-states/2026-09-04/logs/standard.log), [corrected roadmap log](../artifacts/maestro-screen-capture/loading-states/2026-09-04/logs/roadmap-standard.log), [large-text log](../artifacts/maestro-screen-capture/loading-states/2026-09-04/logs/large.log). Center node bands, narrow split geometry and inflated canvas height and setup option-card collapse were corrected before final targeted acceptance captures. [Final roadmap standard run](../artifacts/maestro-screen-capture/loading-states/2026-09-04/logs/roadmap-standard-final.log), [final roadmap/setup large run](../artifacts/maestro-screen-capture/loading-states/2026-09-04/logs/layout-large-final.log).
- **Motion and Reduce Motion: PASS.** Current Home glass motion changes 4,795 of 9,000 pixels inside a skeleton bar between video frames 0s and 1s; with Reduce Motion on, the same bar and all 2,279,340 sampled content pixels remain unchanged. Motion is visible in [the recording](../artifacts/maestro-screen-capture/loading-states/2026-09-04/motion/glass-motion.mp4); isolated bar-pixel comparisons are stored in [moving comparison](../artifacts/maestro-screen-capture/loading-states/2026-09-04/motion/motion-diff.json) and [reduced comparison](../artifacts/maestro-screen-capture/loading-states/2026-09-04/motion/reduced-motion-diff.json). Final targeted layout recaptures use native Reduce Motion on; the remaining matrix captures use motion on. The native reduced preference disables the band, and moving behavior is verified separately. Earlier six-screen motion/lifecycle evidence above remains valid.
- **Independent code QA: PASS.** All delegated work used `gpt-5.6-luna`, reasoning `max`; final bounded delta review found no material issue. Independent final visual QA also passed all 24 corrected max-text roadmap/setup captures in both themes. Accepted approach scores and cleanup decisions are retained in [the completed plan](loading-state-qualification.md).
- **Environment restored.** Expo AppEntry restored byte-for-byte; SHA-256 `b67d02bce1d0d5789c2afcf4243ecfaa479c4a7f6c47c4b8427bd464877f4362`. Standard text, dark appearance and Reduce Motion off restored; normal production entry relaunched. [Restoration record](../artifacts/maestro-screen-capture/loading-states/2026-09-04/bootstrap-restoration.json), [restored app](../artifacts/maestro-screen-capture/loading-states/2026-09-04/restored-app.png).

The component matrix mounts actual exported production components with the real preferences, Screen and retained local headers. Its top controls belong only to the ignored capture entry. Exam and simulation-result captures exclude their native navigator chrome; route options have focused source assertions. This is distinct from the actual production-navigation run. Android, spoken VoiceOver, instrumented performance and every production exam/simulation-result transition were not exercised. The previous full-M2 failures remain unchanged and are not reported as passing. Completed 005 was removed from the active register after this verification.
