appId: com.lkurczab.patternly
name: Patternly visual shell — release-compatible capture
tags:
  - screenshot
  - uiux-evidence
  - release-compatible
---
# This entry point is intended for a clean release install. Production does
# not expose the development-only audit reset marker or reset protocol.
- extendedWaitUntil:
    visible:
      id: "patternly:content:ready"
    timeout: 30000
- runFlow: visual-shell-capture.yaml
