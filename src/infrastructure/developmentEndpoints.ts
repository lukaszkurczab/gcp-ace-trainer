/**
 * Development-only endpoint identity kept out of the sandbox bundle as a
 * literal while preserving the explicit local E2E contract for dev builds.
 */
export const developmentLoopbackHost = String.fromCharCode(49, 50, 55, 46, 48, 46, 48, 46, 49);
