import type { ContentPackagePin } from "../../domain/learning/contentPackagePin";
import { contentPackagePinsEqual } from "../../domain/learning/contentPackagePin";
export type VerifiedSessionCapacity = Readonly<{ packagePin: ContentPackagePin; modeId: string; requestedLength: number; minimumActualLength: number; eligibleItemCount: number; shorteningPolicy: "allowed" | "prohibited" }>;
export type SessionCapacityResolution = Readonly<{ kind: "exact"; actualLength: number }> | Readonly<{ kind: "shortened"; actualLength: number; requestedLength: number }> | Readonly<{ kind: "shortfall"; requestedLength: number; eligibleItemCount: number; missingItemCount: number }>;
export function createVerifiedSessionCapacity(pkg: { packagePin: ContentPackagePin; profile: { configurations: readonly { modeId: string; requestedLengths: readonly number[]; selection: { shortening: string; }; minimumActualLength: number; }[] } }, modeId: string, requestedLength: number, eligibleItemCount: number): VerifiedSessionCapacity { const c = pkg.profile.configurations.find((x) => x.modeId === modeId); if (!c || !c.requestedLengths.includes(requestedLength)) throw new Error(`Mode ${modeId} does not support requested length ${requestedLength}.`); return Object.freeze({ packagePin: pkg.packagePin, modeId, requestedLength, minimumActualLength: c.minimumActualLength, eligibleItemCount, shorteningPolicy: c.selection.shortening === "truthful_to_eligible_count" ? "allowed" : "prohibited" }); }
export function resolveVerifiedSessionCapacity(capacity: VerifiedSessionCapacity, expectedPackagePin: ContentPackagePin): SessionCapacityResolution { if (!contentPackagePinsEqual(capacity.packagePin, expectedPackagePin)) throw new Error("Session capacity package pin does not match the expected package."); if (capacity.eligibleItemCount >= capacity.requestedLength) return { kind: "exact", actualLength: capacity.requestedLength }; if (capacity.shorteningPolicy === "allowed" && capacity.eligibleItemCount >= capacity.minimumActualLength) return { kind: "shortened", actualLength: capacity.eligibleItemCount, requestedLength: capacity.requestedLength }; return { kind: "shortfall", requestedLength: capacity.requestedLength, eligibleItemCount: capacity.eligibleItemCount, missingItemCount: capacity.requestedLength - capacity.eligibleItemCount }; }
