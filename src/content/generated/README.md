# Canonical content build inputs

`canonical-content/` is generated by `npm run sync:content-release` from the current sibling `patternly-content` checkout. It must contain exactly the nine canonical track JSON artifacts and `content-lock.json`.

Do not edit those files manually. Run `npm run check:content-release` to rebuild into a temporary directory and verify byte-for-byte parity without changing application files.
