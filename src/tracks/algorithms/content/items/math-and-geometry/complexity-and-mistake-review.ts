// Planning target: this file should contain questions about complexity and code-review reasoning across math and geometry algorithms:
// logarithmic Euclidean reduction; O(sqrt(n)) trial division and factor enumeration;
// O(n log log n) sieve-style preprocessing under standard assumptions;
// linear digit processing; constant-time coordinate formulas;
// O(rows * columns) matrix transformations;
// preprocessing versus repeated queries; and numeric bit-width caveats.
// It should diagnose mistakes such as calling Euclid O(1),
// describing primality testing through n as O(sqrt(n)),
// ignoring output size when enumerating factors,
// treating formulas with large-integer arithmetic as unit-cost without stating the model,
// calling a matrix rotation O(1),
// or accepting a good Big-O claim despite incorrect numeric semantics.
// Target question count: 10.
// Prefer single_choice, complexity_pair, solution_comparison, code-review, and common-mistake-diagnosis style items.
// Avoid repeating complete arithmetic or geometry traces unless the item specifically tests complexity.
export const complexityAndMistakeReviewQuestions = [];
