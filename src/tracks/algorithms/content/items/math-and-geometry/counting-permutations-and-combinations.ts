// Planning target: this file should contain questions about introductory combinatorial counting in algorithms:
// product and sum rules; ordered versus unordered selections;
// permutations versus combinations; repeated values;
// choosing pairs without double counting; counting complementary cases;
// and deciding whether the task asks for constructions, arrangements, subsets, or index tuples.
// It should teach that the counting formula follows the identity of an outcome and whether order matters.
// It should diagnose mistakes such as using n squared for unordered distinct pairs,
// dividing by two when ordered pairs are required,
// treating duplicate values as distinct without an index-based contract,
// multiplying counts of mutually exclusive cases,
// or using factorial formulas without checking repeated elements and constraints.
// Target question count: 14.
// Prefer single_choice, counting-model selection, solution_comparison, edge_case_drill, and mistake-review style items.
// Avoid advanced combinatorics, inclusion-exclusion as a full curriculum, and large modular binomial computation.
export const countingPermutationsAndCombinationsQuestions = [];
