// Planning target: this file should contain questions about prime testing, factor enumeration, and sieve preprocessing:
// testing divisors only through sqrt(n); paired divisors; perfect squares;
// numbers below two; prime factor multiplicity;
// sieve marking; starting composite marking at p * p;
// and choosing one-query trial division versus many-query preprocessing.
// It should diagnose mistakes such as treating 1 as prime,
// iterating divisors through n,
// emitting a square-root divisor twice,
// stopping factorization after one occurrence,
// marking the prime itself as composite,
// starting every sieve multiple from 2p without understanding repeated work,
// or using a sieve for one small isolated query without justification.
// Target question count: 16.
// Prefer single_choice, solution_comparison, edge_case_drill, factor traces, sieve-state items, and complexity-aware strategy choices.
// Avoid advanced primality tests and segmented sieves.
export const primesFactorsAndSieveQuestions = [];
