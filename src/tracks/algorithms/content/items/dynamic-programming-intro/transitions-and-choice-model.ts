// Planning target: this file should contain questions about deriving a DP transition from legal choices at one state:
// naming each action, identifying its next state, adding its immediate contribution once, combining candidates, and proving every valid solution starts with an enumerated choice.
// It should distinguish state definition from transition and choice enumeration from result aggregation.
// It should diagnose skipped legal choices, incorrect next-state semantics, double-counting, incompatible subproblems, invalid self-dependencies, and copied recurrences.
// Target question count: 20.
// Prefer single_choice, recurrence selection, solution_comparison, subgoal_ordering, edge_case_drill, and small dependency traces.
// Avoid full knapsack, LCS, interval-DP, and tree-DP curricula; use only compact scenarios that isolate transition reasoning.
export const transitionsAndChoiceModelQuestions = [];
