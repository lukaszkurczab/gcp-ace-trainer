// Planning target: this file should contain questions about converting a correct recurrence into top-down memoization:
// forming a complete cache key, checking it before repeated work, storing results before return, handling falsy cached values, sharing one memo, and caching states rather than paths.
// It should teach that memoization removes repeated state evaluation but does not repair an incorrect state or recurrence.
// It should diagnose a memo created per call, truthiness cache checks, caching only base cases, incomplete keys, and incorrect complexity claims.
// Target question count: 16.
// Prefer single_choice, code-review, solution_comparison, edge_case_drill, cache-key selection, and small recursion-DAG traces.
// Avoid language-specific Map trivia unless it directly affects cache correctness.
export const topDownMemoizationQuestions = [];
