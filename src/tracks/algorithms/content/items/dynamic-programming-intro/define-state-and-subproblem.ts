// Planning target: this file should contain questions about defining a DP state and the exact subproblem it represents:
// identifying changing parameters; deciding whether an index, remaining capacity, previous choice, count, or boolean condition belongs in the state;
// writing a precise state sentence; choosing sufficient but non-redundant dimensions; and distinguishing a stored value from state inputs.
// It should teach that two calls can share a memo entry only when every piece of information that can affect the future answer is identical.
// It should diagnose missing state parameters, redundant derived output, undefined dp[i] meaning, and confusing an index with its stored answer.
// Target question count: 20.
// Prefer single_choice, state-definition selection, solution_comparison, edge_case_drill, code-reading, and mistake-review style items.
// Avoid full recurrence derivation, implementation-specific memo syntax, and advanced multidimensional DP families.
export const defineStateAndSubproblemQuestions = [];
