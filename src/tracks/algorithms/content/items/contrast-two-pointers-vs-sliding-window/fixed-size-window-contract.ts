// Planning target: this file should contain questions about fixed-size sliding windows:
// two boundaries representing one contiguous range of exactly k elements; adding the incoming contribution;
// removing the outgoing contribution; recording each complete window; rolling sums, counts, or other removable state;
// and distinguishing this from two independent pointers k positions apart.
// It should diagnose mistakes such as treating left and right as unrelated candidates, forgetting to remove the outgoing contribution,
// shrinking conditionally even though the size contract is fixed, recording an incomplete initial window, or recomputing every window from scratch.
// Target question count: 12.
// Prefer single_choice, solution_comparison, edge_case_drill, subgoal_ordering, and small trace-style items.
// Avoid prefix-sum comparisons, variable-size validity conditions, and advanced frequency-window problems.
export const fixedSizeWindowContractQuestions = [];
