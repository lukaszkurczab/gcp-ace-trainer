// Planning target: this file should contain questions about complexity and code-review reasoning in the two-pointers-versus-sliding-window contrast:
// O(n) convergence when each pointer moves monotonically; amortized O(n) variable-window scans; fixed-window rolling updates;
// O(1) versus state-dependent auxiliary space; sorting preprocessing before two pointers; and correctness before asymptotic comparison.
// It should diagnose mistakes such as calling every nested while loop O(n^2), claiming every two-pointer algorithm is O(n),
// omitting O(n log n) sorting preprocessing, assuming sliding window always uses O(1) state, counting pointer distance as the number of operations,
// or accepting linear complexity despite an invalid movement invariant.
// Target question count: 10.
// Prefer single_choice, solution_comparison, complexity_check, and common-mistake-diagnosis style items.
// Avoid repeating domain traces from earlier files unless the item specifically tests complexity reasoning.
export const complexityAndMistakeReviewQuestions = [];
