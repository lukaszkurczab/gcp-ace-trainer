// Planning target: this file should contain questions about how sorting, original contiguity, and the output contract affect the choice between two pointers and sliding window:
// sorting to enable opposite-end elimination; preserving or destroying original adjacency; subarray or substring requirements;
// subsequence or pair requirements; returning values, original indexes, one pair, one contiguous range, a length, or an in-place transformed prefix.
// It should teach that sorting may enable two pointers but can invalidate a sliding-window problem whose answer depends on original contiguous order.
// It should diagnose mistakes such as sorting before searching for an original subarray, using sliding window for a pair whose members need not be adjacent,
// losing original indexes after sorting, assuming any interval between two pointers is a valid output range, or choosing from Big-O before checking the required result form.
// Target question count: 14.
// Prefer single_choice, solution_comparison, edge_case_drill, output-contract analysis, and mistake-review style items.
// Avoid generic sorting questions and avoid full hash-map-versus-sorting tradeoff analysis.
export const sortingContiguityAndOutputContractQuestions = [];
