// Planning target: this file should contain questions about the relationship between two pointers and sliding window and about the semantic roles of each pointer:
// sliding window as a specialized two-boundary technique; pointers as independent candidates versus window boundaries;
// left/right, slow/fast, read/write, and source/destination roles; and naming the represented region or invariant.
// It should require learners to explain what each pointer means instead of classifying code from variable names.
// It should diagnose mistakes such as treating two pointers and sliding window as disjoint categories,
// treating every same-direction pointer pair as a window, assuming pointer names determine their role,
// or failing to state whether the region between pointers is the active candidate, processed prefix, or discarded area.
// Target question count: 14.
// Prefer single_choice, solution_comparison, invariant-identification, code-reading, and mistake-review style items.
// Avoid full opposite-end pair elimination, compaction implementation, and variable-window shrink traces.
export const taxonomyAndPointerRoleSemanticsQuestions = [];
