// Planning target: this file should contain questions about recognizing the strategy signal in the contrast between hash-based state and sorting:
// keyed lookup; seen-before state; exact frequency counting; complement lookup; index recovery;
// versus sorting to create adjacency, ordered runs, deterministic traversal, or a canonical representation.
// It should teach the learner to name what structure the solution needs before comparing Big-O.
// It should diagnose mistakes such as choosing a hash map only because the task mentions duplicates,
// choosing sorting only because values can be ordered,
// assuming expected O(n) automatically makes hashing the best solution,
// or failing to distinguish keyed access from order-producing preprocessing.
// Target question count: 14.
// Prefer single_choice, strategy_choice, solution_comparison, and mistake-review style items.
// Avoid detailed hash-table implementation, detailed sorting mechanics, complement traces, and full complexity accounting; those belong in later files.
export const recognizeHashMapVsSortingStrategySignalQuestions = [];
