// Planning target: this file should contain questions about recursive depth-first traversal:
// entering and marking a vertex, exploring unvisited neighbors, branch return, recursion-stack meaning, and global traversal versus path backtracking.
// It should diagnose recursion before marking, missing visited guards, unmarking global state, early return after one neighbor, distance confusion, and assumed unique order.
// Target question count: 18.
// Prefer single_choice, solution_comparison, edge_case_drill, code-review, subgoal-ordering, and small call-stack traces.
// Avoid DFS timestamps, SCC algorithms, bridge detection, and exhaustive simple-path enumeration.
export const recursiveDfsAndCallStateQuestions = [];
