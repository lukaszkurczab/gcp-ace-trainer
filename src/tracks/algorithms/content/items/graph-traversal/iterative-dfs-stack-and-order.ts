// Planning target: this file should contain questions about iterative DFS:
// LIFO stack behavior, push/pop order, preventing duplicate scheduling, explicit recursion state, and neighbor push order's visible effect.
// It should teach that iterative and recursive DFS can reach the same vertices without emitting the same order.
// It should diagnose FIFO behavior, mistaken adjacency-order assumptions, duplicate pushes, forced identical orders, and insufficient frame state.
// Target question count: 14.
// Prefer single_choice, solution_comparison, edge_case_drill, code-review, and small stack traces.
// Avoid advanced explicit-stack simulations of timestamps and postorder algorithms.
export const iterativeDfsStackAndOrderQuestions = [];
