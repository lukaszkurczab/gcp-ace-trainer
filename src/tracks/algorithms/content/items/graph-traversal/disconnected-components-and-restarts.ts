// Planning target: this file should contain questions about disconnected graphs:
// source-reachable traversal versus full coverage, scanning vertices, restarting from unvisited vertices, component counts and sizes, and isolated vertices.
// It should teach that one traversal covers only the component containing its start.
// It should diagnose single-start coverage assumptions, per-vertex component increments, visited resets, skipped isolates, and redundant restarts.
// Target question count: 16.
// Prefer single_choice, solution_comparison, edge_case_drill, component-count traces, and subgoal-ordering items.
// Avoid strongly connected components and advanced connectivity algorithms.
export const disconnectedComponentsAndRestartsQuestions = [];
