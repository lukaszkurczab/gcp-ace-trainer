// Planning target: this file should contain questions about graph-traversal complexity and code review:
// O(V + E) adjacency-list traversal, O(V²) matrix neighbor scans, auxiliary traversal state, edge-count conventions, and reachable versus full graphs.
// It should diagnose ignored edge inspection, needless V×E multiplication, universal matrix claims, ignored recursion depth, ambiguous undirected counts, and duplicate scheduling.
// Target question count: 12.
// Prefer single_choice, complexity_pair, solution_comparison, code-review, and common-mistake-diagnosis style items.
// Avoid traversal traces unless specifically testing complexity reasoning.
export const complexityAndMistakeReviewQuestions = [];
