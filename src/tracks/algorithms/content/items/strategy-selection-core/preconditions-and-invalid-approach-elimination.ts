// Planning target: this file should contain questions about verifying required preconditions and rejecting strategies that cannot be proven correct:
// sortedness; monotonicity; non-negative values; static input; acyclic structure; unique identity; optimal substructure;
// overlapping subproblems; removable window state; greedy exchange conditions; stable ordering; bounded domains; and legal mutation.
// It should teach that a fast approach is invalid when its correctness assumptions are absent.
// It should diagnose mixed-sign sliding-window target sums without monotonicity, opposite-end pointers on unsorted data,
// ordinary prefix subtraction for a non-invertible aggregate, binary search on a non-monotonic predicate,
// greedy based only on local attractiveness, or treating a graph as a tree without cycle protection.
// Target question count: 20. This is the central file of the topic.
// Prefer single_choice, missing-precondition identification, counterexample selection, invalid-approach diagnosis, and solution comparison.
// Avoid detailed execution once an approach has been rejected.
export const preconditionsAndInvalidApproachEliminationQuestions = [];
