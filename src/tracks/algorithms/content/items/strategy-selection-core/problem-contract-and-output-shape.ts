// Planning target: this file should contain questions about extracting the exact problem contract before choosing an algorithm:
// existence versus counting; minimum versus maximum; one result versus all results;
// values versus indexes; contiguous ranges versus subsequences or subsets;
// exact output order; mutation requirements; and whether duplicates represent distinct values or distinct positions.
// It should teach that superficially similar prompts may require different strategies because their output contracts differ.
// It should diagnose mistakes such as solving existence when the task asks for a count,
// returning values when original indexes are required, treating a subsequence problem as a contiguous-window problem,
// ignoring uniqueness requirements, or optimizing the wrong quantity.
// Target question count: 16.
// Prefer single_choice, contract-selection, output-shape comparison, ambiguity diagnosis, and mistake-review style items.
// Use short realistic problem fragments rather than full multi-stage interview problems. Avoid detailed algorithm execution.
export const problemContractAndOutputShapeQuestions = [];
