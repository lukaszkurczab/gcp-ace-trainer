// Planning target: this file should contain questions about evaluating candidate approaches in the correct order:
// contract satisfaction; required assumptions; correctness argument; edge-case behavior; then time and space complexity.
// It should teach that an O(n) approach is worse than an O(n log n) approach when the O(n) approach is not valid for the input domain.
// It should diagnose preferring lower Big-O without checking correctness, accepting a strategy because it passes the sample,
// rejecting a correct approach only because a theoretically faster one exists, confusing expected and worst-case guarantees,
// or treating a complexity claim as evidence of correctness.
// Target question count: 14.
// Prefer single_choice, solution ranking, correctness-versus-performance comparison, counterexample analysis, and mistake-review style items.
// Avoid implementation details unrelated to strategic evaluation.
export const correctnessBeforeComplexityQuestions = [];
