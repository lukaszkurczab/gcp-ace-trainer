// Planning target: this file should contain questions about identifying the invariant or progress measure that makes an approach implementable and provably terminating:
// processed-prefix invariants; valid-window invariants; sorted-region invariants; stack-top meaning; queue-front meaning;
// subtree result contracts; recursive progress; binary-search interval meaning; heap candidate validity; and monotonic pointer movement.
// It should teach that the invariant explains both correctness and what each loop iteration may do next.
// It should diagnose naming variables without defining their meaning, moving a pointer without identifying eliminated candidates,
// using recursion without a decreasing measure, claiming a window is valid without specifying the predicate,
// or changing the meaning of maintained state during the algorithm.
// Target question count: 14.
// Prefer single_choice, invariant selection, progress-measure selection, solution comparison, and mistake-review style items.
// Avoid detailed code-level traces.
export const invariantsAndProgressMeasuresQuestions = [];
