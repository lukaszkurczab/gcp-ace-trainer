// Planning target: this file should contain questions about identifying ambiguities that materially affect algorithm choice:
// whether input is sorted; whether values may be negative; whether duplicates count separately; whether indexes are required;
// whether mutation is allowed; whether queries are repeated; whether the structure may contain cycles; whether output order matters;
// and whether constraints are large enough to exclude brute force.
// It should teach users to ask the smallest useful clarifying question or state an explicit assumption when information is missing.
// It should diagnose asking irrelevant implementation questions, silently assuming non-negative values, assuming unique elements,
// asking for details that do not change the approach, or selecting an algorithm before resolving a contract-changing ambiguity.
// Target question count: 10.
// Prefer single_choice, clarifying-question selection, assumption diagnosis, and interview-response style items.
// Keep scenarios short and focused. Avoid full conversational interview simulations.
export const ambiguityClarifyingQuestionsAndAssumptionsQuestions = [];
