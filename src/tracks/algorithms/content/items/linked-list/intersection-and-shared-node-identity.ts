// Planning target: this file should contain questions about intersection of singly linked lists by shared node identity:
// shared suffixes; aligning lengths; switching heads; distinguishing equal values from the same node;
// handling no intersection; and understanding why acyclic lists that intersect share every later node.
// It should diagnose mistakes such as comparing node values, returning the first equal-valued pair,
// resetting only one pointer incorrectly, assuming lists must have equal lengths,
// modifying the lists to mark nodes without permission, or treating crossing visual paths without shared references as intersection.
// Target question count: 12.
// Prefer single_choice, solution_comparison, edge_case_drill, identity reasoning, and small reference traces.
// Avoid cyclic-list intersection and hash-set-based solutions as the dominant curriculum.
export const intersectionAndSharedNodeIdentityQuestions = [];
