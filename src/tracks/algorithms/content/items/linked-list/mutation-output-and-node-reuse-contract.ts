// Planning target: this file should contain questions about mutation and output contracts in linked-list algorithms:
// modifying the original list versus returning a copy; reusing existing nodes;
// allocating dummy nodes only as temporary scaffolding; preserving original node identity;
// returning a new head; keeping or discarding detached nodes; and satisfying stable-order requirements.
// It should diagnose mistakes such as returning the original head after head replacement,
// creating value copies when identity must be preserved, claiming O(1) space while allocating a copied node per input node,
// mutating input when the contract forbids it, returning a sentinel as part of the result,
// or preserving values while breaking required node identity.
// Target question count: 10.
// Prefer single_choice, solution_comparison, output-contract analysis, edge_case_drill, and mistake-review style items.
// Avoid generic immutability discussions unrelated to linked-list structure.
export const mutationOutputAndNodeReuseContractQuestions = [];
