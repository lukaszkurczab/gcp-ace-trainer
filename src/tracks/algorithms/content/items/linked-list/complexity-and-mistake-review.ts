// Planning target: this file should contain questions about complexity and code-review reasoning for linked-list algorithms:
// O(n) traversal; O(1) local pointer rewiring; O(n + m) merge and intersection scans;
// recursion-stack space; repeated rescanning that creates O(n^2); and the lack of constant-time random access.
// It should diagnose mistakes such as calling access to the kth node O(1), ignoring recursion stack space,
// assuming nested pointer loops are always quadratic, rescanning from head for each position,
// claiming copied-node solutions use O(1) auxiliary space,
// or accepting a good complexity claim despite a lost suffix or broken pointer invariant.
// Target question count: 10.
// Prefer single_choice, complexity_pair, solution_comparison, code-review, and common-mistake-diagnosis style items.
// Avoid repeating full pointer traces unless the item specifically tests complexity.
export const complexityAndMistakeReviewQuestions = [];
