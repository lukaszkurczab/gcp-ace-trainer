// Planning target: this file should contain questions about basic linked-list traversal and boundary state:
// starting from head; advancing with current = current.next; stopping at null;
// handling empty and single-node lists; identifying head and tail;
// updating head or tail when the structure changes; and avoiding dereferencing null.
// It should diagnose mistakes such as skipping the head, stopping before processing the tail,
// reading current.next when current is null, assuming head always remains unchanged,
// retaining an obsolete tail after appending or truncating,
// or using index-based assumptions without traversal.
// Target question count: 14.
// Prefer single_choice, solution_comparison, edge_case_drill, subgoal-ordering, and small traversal traces.
// Avoid full insertion, deletion, reversal, and cycle detection mechanics.
export const traversalHeadTailAndNullContractQuestions = [];
