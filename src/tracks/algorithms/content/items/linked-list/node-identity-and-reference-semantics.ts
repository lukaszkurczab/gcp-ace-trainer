// Planning target: this file should contain questions about node identity and reference semantics:
// two nodes with equal values but different identities; multiple references to the same node;
// shared suffixes; assigning references versus copying values;
// and how changing next mutates the structure visible through every reference to that node.
// It should teach that linked-list algorithms often operate on node identity, not only stored values.
// It should diagnose mistakes such as treating equal values as the same node,
// comparing intersection by value, assuming assigning one node variable copies the node,
// mutating a shared node without considering all list heads,
// or losing track of which reference points to the original structure.
// Target question count: 14.
// Prefer single_choice, reference-trace, solution_comparison, edge_case_drill, and mistake-review style items.
// Avoid language-runtime internals beyond the explicit reference model.
export const nodeIdentityAndReferenceSemanticsQuestions = [];
