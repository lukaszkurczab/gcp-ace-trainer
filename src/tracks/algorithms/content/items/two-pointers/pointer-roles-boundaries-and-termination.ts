// Planning target: define pointer roles: left/right boundaries, read/write positions, fast/slow progression, processed regions, boundary conventions, and valid termination.
// Each pointer must correspond to a stable invariant; diagnose crossing, out-of-range access, premature termination, and changing pointer meaning mid-algorithm.
// Target question count: 14. Avoid family-specific pair-search and compaction details.
export const pointerRolesBoundariesAndTerminationQuestions = [];
