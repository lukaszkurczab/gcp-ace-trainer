// Planning target: preserve original identity through sorting and satisfy contracts for indexes, values, counts, lengths, mutated prefixes, input order, copying, and ties.
// Diagnose returning sorted indexes, unintended mutation, arbitrary results under an earliest-result contract, and treating a compacted suffix as meaningful output.
// Target question count: 12. Avoid generic API design unrelated to pointer correctness.
export const originalIndexOutputAndMutationContractQuestions = [];
