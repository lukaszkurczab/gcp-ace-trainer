// Planning target: this file should contain questions about choosing what a stack stores:
// values; indexes; value-index pairs; and the information needed for output values, positions, distances, spans, or duplicate identity.
// It should teach that indexes preserve both value access and positional distance, while values alone may lose occurrence identity.
// It should diagnose mistakes such as storing only values when the result requires a distance,
// subtracting values instead of indexes,
// losing duplicate occurrence identity,
// storing indexes but comparing them directly as values,
// or carrying unnecessary state when only the qualifying value is required.
// Target question count: 12.
// Prefer single_choice, representation-choice, solution_comparison, edge_case_drill, and small trace-style items.
// Avoid generic array-index questions and avoid advanced contribution-width calculations.
export const indicesValuesAndDistanceContractQuestions = [];
