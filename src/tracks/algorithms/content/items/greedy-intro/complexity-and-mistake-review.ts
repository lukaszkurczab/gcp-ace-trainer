// Planning target: this file should contain questions about greedy complexity and code review:
// sorting plus scans, heap-based selection, auxiliary state, preprocessing costs, repeated feasibility checks, and correctness before asymptotics.
// It should diagnose O(n) claims after sorting, universal linear-greedy claims, ignored heap work, hidden local-choice cost, and unproven fast strategies.
// Target question count: 10.
// Prefer single_choice, complexity_pair, solution_comparison, code-review, and common-mistake-diagnosis style items.
// Avoid repeating full domain scenarios unless specifically testing complexity reasoning.
export const complexityAndMistakeReviewQuestions = [];
