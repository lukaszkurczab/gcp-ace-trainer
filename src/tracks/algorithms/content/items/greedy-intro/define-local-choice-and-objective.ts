// Planning target: this file should contain questions about defining exact greedy rules and linking them to the global objective:
// legal candidates, concrete best-now scores, value/cost/ratio/finish/reach/slack distinctions, commitment timing, and optimization goals.
// It should teach that a rule must choose between concrete candidates rather than merely say "take the best".
// It should diagnose undefined scores, irrelevant local metrics, infeasible candidates, incompatible rules, and opportunistic mid-scan rule changes.
// Target question count: 18.
// Prefer single_choice, rule-selection, solution_comparison, code-reading, edge_case_drill, and mistake-review style items.
// Avoid full correctness proofs and family-specific implementation templates.
export const defineLocalChoiceAndObjectiveQuestions = [];
