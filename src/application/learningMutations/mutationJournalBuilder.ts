import type { MutationJournalRecord, JournalWrite } from "../../storage/repositories/mutationJournalRepository";
import { createIdentityFingerprint } from "./identity";
export type MutationOperation = MutationJournalRecord["operation"];
export async function buildMutationJournal(input: { operation: MutationOperation; sessionId: string; trackId: string; identity: unknown; writes: readonly JournalWrite[]; createdAt: string }): Promise<MutationJournalRecord> { const inputFingerprint = await createIdentityFingerprint([input.operation, input.sessionId, input.trackId, input.identity]); return { journalId: `journal:${inputFingerprint}`, operation: input.operation, status: "prepared", createdAt: input.createdAt, sessionId: input.sessionId, trackId: input.trackId, inputFingerprint, writes: input.writes }; }
