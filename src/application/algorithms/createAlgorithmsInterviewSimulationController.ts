import { AlgorithmsInterviewSimulationController } from "./AlgorithmsInterviewSimulationController";

export function createAlgorithmsInterviewSimulationController(): AlgorithmsInterviewSimulationController {
  throw new Error("Algorithms Interview Simulation is unavailable until the canonical family runtime is installed.");
}
